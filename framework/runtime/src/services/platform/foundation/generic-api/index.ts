/**
 * Generic API Module
 *
 * Provides generic data access capabilities:
 * - Cross-entity queries with joins
 * - Query DSL for safe, declarative queries
 * - Security enforcement and field-level access control
 * - Observability (metrics, tracing)
 */

export * from "./query/index.js";
