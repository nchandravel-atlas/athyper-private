import { StubPage } from "@/components/stubs/StubPage";

export default function IntegrationsPage() {
    return <StubPage epic={6} title="Integrations" description="View integration status and external connections." route="/app/:entity/:id/integrations" />;
}
