import { StubPage } from "@/components/stubs/StubPage";

export default function EntityConfigurationPage() {
    return <StubPage epic={7} title="Entity Configuration" description="Configure entity settings, fields, and behavior." route="/wb/admin/meta/entities/:entity" />;
}
