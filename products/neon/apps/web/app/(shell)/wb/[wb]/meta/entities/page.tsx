import { StubPage } from "@/components/stubs/StubPage";

export default function EntityGovernancePage() {
    return <StubPage epic={7} title="Entity Governance" description="Manage entity definitions, forms, views, fields, policies, and workflows." route="/wb/admin/meta/entities" />;
}
