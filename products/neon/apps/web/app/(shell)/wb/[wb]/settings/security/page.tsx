import { StubPage } from "@/components/stubs/StubPage";

export default function SecuritySettingsPage() {
    return <StubPage epic={8} title="Security Settings" description="Manage your security settings and active sessions." route="/wb/:wb/settings/security" />;
}
